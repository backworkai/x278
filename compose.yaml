name: x278-realistic

services:
  payer-api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUN_IMAGE: ${BUN_IMAGE:-oven/bun:1-alpine@sha256:5acc90a93e91ff07bf72aa90a7c9f0fa189765aec90b47bdbf2152d2196383c0}
    image: backwork/x278-realistic:local
    command: ["bun", "run", "payer:http"]
    init: true
    environment:
      X278_PAYER_PORT: "8787"
    ports:
      - "${X278_PAYER_PORT:-8787}:8787"
    healthcheck:
      test:
        [
          "CMD",
          "bun",
          "--eval",
          "fetch('http://127.0.0.1:8787/healthz').then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"
        ]
      interval: 2s
      timeout: 1s
      retries: 20
      start_period: 5s

  provider-scenario:
    image: backwork/x278-realistic:local
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUN_IMAGE: ${BUN_IMAGE:-oven/bun:1-alpine@sha256:5acc90a93e91ff07bf72aa90a7c9f0fa189765aec90b47bdbf2152d2196383c0}
    command: ["bun", "run", "scenario:realistic"]
    init: true
    environment:
      X278_PAYER_URL: "http://payer-api:8787"
    depends_on:
      payer-api:
        condition: service_healthy

  hapi-fhir:
    image: ${HAPI_FHIR_IMAGE:-hapiproject/hapi@sha256:1be4d7ffe7a35a9fb46151851e5a20b25c5016f16c8ef8b59b0c807ad06a40c1}
    profiles: ["fhir"]
    init: true
    ports:
      - "${HAPI_FHIR_PORT:-8090}:8080"

  provider-fhir-scenario:
    image: backwork/x278-realistic:local
    profiles: ["fhir"]
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUN_IMAGE: ${BUN_IMAGE:-oven/bun:1-alpine@sha256:5acc90a93e91ff07bf72aa90a7c9f0fa189765aec90b47bdbf2152d2196383c0}
    command: ["bun", "run", "scenario:fhir"]
    init: true
    environment:
      X278_PAYER_URL: "http://payer-api:8787"
      FHIR_BASE_URL: "http://hapi-fhir:8080/fhir"
    depends_on:
      payer-api:
        condition: service_healthy
      hapi-fhir:
        condition: service_started
